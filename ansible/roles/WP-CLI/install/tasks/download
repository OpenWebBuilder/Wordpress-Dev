---

- name: Download WP-CLI
  become: true
  ansible.builtin.get_url:
    url: "{{download_url}}"
    dest: "{{dest}}"
    mode: '0740'

- name: Copy to path/wp
  become: true
  ansible.builtin.copy:
    src: "{{dest/wp-cli.phar}}"
    dest: "{{to}}"
    remote_src: yes
